//backend logic

//player constructor
var Player = function(name, points) {
  this.name = name,
  this.points = points
};

var random = Math.ceil(Math.random()*6);
console.log(random)

















//frontend logic
